kind: Service
apiVersion: v1
metadata:
  name: revalyze_landing
  namespace: revalyze
spec:
  type: NodePort
  selector:
    app: revalyze_landing
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
